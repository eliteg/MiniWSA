package org.example.miniwsa.event;

/** Rule severity (assignment domain model). Drives the base threat score in enrichment (v0.3). */
public enum Severity {
    CRITICAL,
    HIGH,
    MEDIUM,
    LOW
}
